# Local editor / agent settings
.claude/
.claude/settings.local.json
.idea/
.vscode/
.DS_Store
Thumbs.db
next_plan/

# Agent operating-rule files (internal, not for the public repo)
AGENTS.md
CLAUDE.md

# Build artifacts (regenerate with `python -m build`; publish via GitHub Releases)
dist/
build/
*.egg-info/

# Local runtime cache and generated artifacts
.bundleInspector/
.pytest_cache/
.tmp_test_artifacts/
tests/.tmp_sandbox/
api_map.json
api_map.txt
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Python cache artifacts
**/__pycache__/
*.py[cod]
.venv/
venv/
env/


# Scan output (never commit target scan data)
artifacts/

# CLI scan output (never commit)
bundleInspector_*report*.json
bundleInspector_*report*.html
bundleInspector_local_report.*
